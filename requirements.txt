# Core
numpy==2.2.6
requests>=2.28.0

# Claude API
anthropic>=0.76.0

# MCP server (Claude Code integration)
mcp[cli]>=1.2.0,<2


# Video comprehension (local, offline — transcription/)
# faster-whisper pulls in ctranslate2, av, onnxruntime, tokenizers, huggingface-hub
faster-whisper>=1.2.0
Pillow>=10.0.0

# Testing
pytest>=9.0.2

